export kubever=$(kubectl version | base64 | tr -d '\n')

ubuntu@ip-172-31-39-7:~$ kubectl apply -f "https://cloud.weave.works/k8s/net?k8sversion=$kubever" 


eksctl create cluster \
--name myk8s \
--region ap-south-1 \
--nodes 3 \
--node-type t2.micro \
--with-oidc \
--ssh-access \
--nodes-min 2 \
--nodes-max 5 \
--ssh-public-key testnew \
--managed
